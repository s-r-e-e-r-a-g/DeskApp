package com.example.aipocketassistant.audio

class AudioRecorder {
    // Stub for now. We are using AndroidSpeechProvider which manages AudioRecord internally.
    // If we switch to a remote STT service (e.g. Whisper API directly), we will implement AudioRecord capture here.
}
